services:
  - type: web
    name: movu-back
    plan: free
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        value: ""
      - key: MONGO_URI
        value: ""
      - key: EMAIL_HOST
        value: ""
      - key: EMAIL_PORT
        value: ""
      - key: EMAIL_USER
        value: ""
      - key: EMAIL_PASS
        value: ""
