# Example .env file for Movu_back
# Copy this to a file named .env and update values for your environment.

# MongoDB connection string (replace with your Atlas URI or local DB)
# For local development you can use the fallback: mongodb://127.0.0.1:27017/movu_back
MONGO_URI=

# Other environment variables can go here, e.g.:
# PORT=3000
# JWT_SECRET=your_jwt_secret
